import { host } from "./host";

export const getAllAlertApi = () => (
    `${host}/api/alert/all`
)

// export const addCameraApi = () => (
//     `${host}/api/camera`
// )

// export const updateCameraApi = () => (
//     `${host}/api/camera`
// )

// export const deleteCameraApi = (cameraId) => (
//     `${host}/api/camera?id=${cameraId}`
// )